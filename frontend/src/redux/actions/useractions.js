// we need tyo store the info of the user and throught this we can 
//fetch the infos
// what can user do in store ,
//can set the user and can get the user
// to put the data in store and to read the date from the store 
const setUser = {
    type: 'SET_USER',
}

const getUser = {
    type: 'GET_USER',
}

export {setUser, getUser};